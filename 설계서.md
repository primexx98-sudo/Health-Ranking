# 건강기능식품 판매순위 크롤러 설계서

최초 작성: 2026-06-25
최종 수정: 2026-06-26 (카테고리 분류 시스템 도입 / 크롤러 전면 개선 / Actions 권한 수정 / 네이버 DataLab 트렌드 조사 추가)

---

## 1. 프로젝트 개요

올리브영 · 다이소몰 · 카카오 선물하기 건강기능식품 판매순위 TOP10을
매일 자동 수집하여 일별 엑셀로 저장하고, 월별 평균순위 TOP10을 집계한다.
상품을 카테고리로 자동 분류하여 일별·월별 카테고리 비율 통계 시트를 함께 생성한다.
PC가 꺼진 상태에서도 GitHub Actions가 3개 플랫폼 모두 자동 수집한다.

---

## 2. GitHub 저장소

| 항목 | 값 |
|------|----|
| 리포 | https://github.com/primexx98-sudo/Health-Ranking (Private) |
| 계정 | primexx98-sudo |
| 이메일 | primexx98@gmail.com |
| 브랜치 | main |
| 로컬 경로 | C:\Users\thefuture_brand\Desktop\업무\시장조사 |

> PAT(Personal Access Token)은 `workflow` + `repo` 권한 필요.
> 분실 시 GitHub → Settings → Developer settings → Personal access tokens 에서 재발급.

---

## 3. 수집 대상

| 플랫폼 | URL | 카테고리 | 수집 범위 |
|--------|-----|----------|-----------|
| 카카오 선물하기 | https://gift.kakao.com/category/5/subcategory/99 | 건강식품·영양제 | 추천순 TOP 10 (MD추천 광고 제외) |
| 카카오 선물하기 | https://gift.kakao.com/category/5/subcategory/100 | 다이어트·이너뷰티 | 추천순 TOP 10 (MD추천 광고 제외) |
| 다이소몰 | https://www.daisomall.co.kr/ds/exhCtgr/C208/CTGR_00022/CTGR_01020 | 건강식품 | 실시간 랭킹 TOP 10 |
| 올리브영 | https://www.oliveyoung.co.kr/store/main/getBestList.do?dispCatNo=900000100100001&fltDispCatNo=10000020001&pageIdx=1&rowsPerPage=24&... | 건강식품 | 판매랭킹 TOP 10 |

---

## 4. 수집 항목 (엑셀 컬럼)

| 컬럼 | 설명 |
|------|------|
| 카테고리 | 상품 카테고리 (classifier.py 키워드 자동 분류) |
| 순위 | 1~10 |
| 상품명 | 제품 이름 |
| 브랜드 | 제조사/브랜드명 |
| 가격 | 판매가 (예: 42,900원) |
| 상품URL | 상품 상세 페이지 링크 |

---

## 5. 파일 구조

```
C:\Users\thefuture_brand\Desktop\업무\시장조사\   ← 로컬 & GitHub 리포 루트
│
├── .github/workflows/
│   ├── daily_crawl.yml          # 매일 09:00 KST 자동 실행 (permissions: contents: write 포함)
│   └── monthly_aggregate.yml    # 매월 1일 09:00 KST 자동 실행 (permissions: contents: write 포함)
│
├── crawlers/
│   ├── __init__.py
│   ├── config.py                # ★ URL 변경 시 이 파일만 수정
│   ├── classifier.py            # ★ 카테고리 추가/수정 시 이 파일만 수정
│   ├── base.py                  # Playwright 공통 유틸 (new_page, save_screenshot, clean_price)
│   ├── kakao.py                 # 카카오 크롤러 — gc-product 웹컴포넌트, txt_prdbrand/txt_prdname, 광고 필터
│   ├── daiso.py                 # 다이소 크롤러 — detail-link 카드, 상세페이지 브랜드 보강
│   └── oliveyoung.py            # 올리브영 크롤러 — curl_cffi + BeautifulSoup (Cloudflare 우회)
│
├── data/
│   ├── daily/   YYYY-MM-DD.xlsx           # 일별 수집본 (시트 4개)
│   └── monthly/ YYYY-MM_월별취합.xlsx      # 월별 평균순위 (시트 4개)
│
├── screenshots/                 # 카카오·다이소 디버그 스크린샷 (Actions Artifact로 업로드)
├── main.py                      # 일별 크롤링 실행 (GitHub Actions + 로컬 공용)
├── monthly_aggregate.py         # 월별 평균순위 집계
├── app.py                       # Streamlit 대시보드 (오늘 랭킹 / 월별 리포트 / 수동 수집)
├── requirements.txt
├── 설계서.md
└── README.md
```

---

## 6. 카테고리 분류 시스템 (`crawlers/classifier.py`)

### 분류 방식
- **키워드 우선순위 기반 단일 분류**: 상품명 + 브랜드명을 합쳐 소문자 변환 후 키워드 순서대로 매칭
- 가장 먼저 매칭되는 카테고리 1개만 배정, 미매칭 시 `기타/특수목적`

### 현재 카테고리 목록 (우선순위 순)

| 순위 | 카테고리 | 주요 키워드 |
|------|---------|------------|
| 1 | 멀티비타민/종합비타민 | 멀티비타민, 종합비타민, 이뮨비타민 |
| 2 | 비타민C/항산화 | 비타민c, c1000, 항산화, 아스코르브 |
| 3 | 비타민D | 비타민d, 비타민디 |
| 4 | 마그네슘 | 마그네슘 |
| 5 | 루테인/눈건강 | 루테인, 제아잔틴, 눈건강, 오메가3 |
| 6 | 혈행 | 코엔자임, q10, 코큐텐, 혈행, 혈액순환 |
| 7 | 홍삼/인삼 | 홍삼, 인삼, 고려홍삼, 정관장 |
| 8 | 다이어트/체중관리 | 다이어트, 슬리밍, 닭가슴살, 단백질, 크레아틴, 혈당, 바나바 |
| 9 | 프로바이오틱스/유산균 | 프로바이오틱스, 유산균, 락토바실러스, 비피더스 |
| 10 | 피부/미용 | 콜라겐, 피부, 히알루론산, 비오틴, 이너뷰티, 더마 |
| 11 | 기타/특수목적 | (위 카테고리 미해당 전부) |

### 카테고리 추가/수정 방법
`crawlers/classifier.py`의 `CATEGORY_MAP` 리스트에 항목 추가 또는 키워드 수정.
우선순위는 리스트 순서 = 위에 있을수록 먼저 매칭.

```python
# 예: 오메가3 별도 카테고리 분리 시
("오메가3/EPA·DHA", ["오메가3", "오메가-3", "omega3", "epa", "dha"]),
```

---

## 7. 크롤러 방식 (플랫폼별)

| 플랫폼 | 라이브러리 | 추출 방식 | Cloudflare | 수집 수 |
|--------|-----------|-----------|-----------|---------|
| 카카오 | Playwright (headless) | JS — `gc-product` 웹컴포넌트, `span.txt_prdbrand`, `strong.txt_prdname` | 없음 | 카테고리당 10개 × 2 = 최대 20개 |
| 다이소 | Playwright (headless) | JS — `a.detail-link[href]` 카드 수집 + 상세페이지 `a.brand-area .detail-title` 브랜드 추출 | 없음 | 10개 |
| 올리브영 | **curl_cffi** + BeautifulSoup | CSS 선택자 — `ul.cate_prd_list > li`, `span.tx_brand`, `p.tx_name` | **IP 차단 → TLS 모방으로 우회** | 10개 |

### 카카오 수집 로직 상세

- 셀렉터 우선순위:
  1. `gc-product` 웹 컴포넌트 직접 선택
  2. `a.link_prdunit` 가장 많은 `.list_prd` 컨테이너의 자식 요소
- 브랜드: `span.txt_prdbrand`
- 상품명: `strong.txt_prdname`
- 링크: `a.link_prdunit[href]` → `https://gift.kakao.com` 접두
- **MD추천 광고 제외**: `.area_ad`, `.txt_ad`, `gc-product-ad-badge` 포함 아이템 skip

### 다이소 수집 로직 상세

- 카드 탐색: `a.detail-link[href]` 중 `pdNo=` 파라미터 있는 것만
- `swiper-slide-duplicate`(루프 복제 슬라이드) 제외, `pdNo` 기준 중복 제거
- 10개 미만이면 `.swiper-button-next` 클릭 fallback (최대 6회)
- **브랜드 보강**: 목록 카드에 브랜드 없음 → TOP10 상세페이지 10회 방문 → `a.brand-area .detail-title` 추출
  - 브랜드명이 상품명 앞에 중복으로 붙어있으면 자동 제거
  - (상세페이지 10회 추가 로딩 → 다소 느림)

### 올리브영 Cloudflare 우회 원리

- 일반 `requests` → **403** (Azure IP 포함 클라우드 IP 전체 차단)
- `curl_cffi` + `impersonate="chrome124"` → **200 성공**
- Chrome의 TLS 핑거프린트(JA3/JA4)를 완벽히 모방 → 봇 감지 통과
- HTML 파싱: BeautifulSoup + lxml (`ul.cate_prd_list > li` CSS 선택자)
- `rowsPerPage=24`로 충분한 후보 확보 후 TOP 10 추출

---

## 8. Streamlit 대시보드 (`app.py`)

실행: `streamlit run app.py` → http://localhost:8501

| 메뉴 | 내용 |
|------|------|
| **오늘 랭킹** | 플랫폼별 탭 / 어제 대비 순위 변동(▲▼🆕) / 신규·상승·하락 요약 |
| **월별 리포트** | 월 선택 → 평균순위 TOP10 / 전월 미등장 신규 상품 🆕 하이라이트 |
| **수동 수집** | 즉시 수집 버튼 / 수집 후 GitHub Push |

> 오늘 data/daily/YYYY-MM-DD.xlsx 파일이 없으면 "수동 수집" 메뉴로 안내

---

## 9. 자동화 (GitHub Actions)

| 워크플로 | 트리거 | 동작 |
|---------|--------|------|
| `daily_crawl.yml` | 매일 UTC 00:00 (= KST 09:00) + 수동 | 3개 플랫폼 수집 → 카테고리 분류 → xlsx 저장 → git push |
| `monthly_aggregate.yml` | 매월 1일 UTC 00:00 + 수동 | 전월 daily xlsx 집계 → 카테고리 통계 → 월별 xlsx 저장 → git push |

**실행 환경:** `ubuntu-22.04`
> ubuntu-latest(=24.04)는 Playwright libasound2 호환 문제로 반드시 22.04 고정

**권한 설정 (필수):** 두 워크플로 모두 `permissions: contents: write` 포함
> 누락 시 git push 단계에서 권한 오류 발생 → 데이터가 GitHub에 저장되지 않음

**수동 실행:**
1. https://github.com/primexx98-sudo/Health-Ranking/actions 접속
2. 왼쪽 워크플로 선택 → **Run workflow** 클릭

**실패 확인:**
- Actions 로그에서 오류 메시지 확인
- `screenshots/` 폴더가 Artifact로 업로드됨 (실행 결과 페이지 하단 다운로드)

---

## 10. 로컬 실행

### 환경 설치 (최초 1회)
```bash
cd C:\Users\thefuture_brand\Desktop\업무\시장조사
pip install -r requirements.txt
playwright install chromium
```

### 일별 수집
```bash
python main.py               # 오늘 날짜로 저장
python main.py TEST          # TEST.xlsx로 저장 (테스트용)
```

### 대시보드 실행
```bash
streamlit run app.py
# http://localhost:8501
```

### 월별 집계 수동 실행
```bash
python monthly_aggregate.py
```

### Git push
```bash
git add data/
git commit -m "data: YYYY-MM-DD 수집"
git pull --rebase
git push
```

> 엑셀 파일이 열려있으면 PermissionError 발생 → 파일 닫고 재실행

---

## 11. 엑셀 파일 구조

### 일별 `data/daily/YYYY-MM-DD.xlsx` — 시트 4개

| 시트명 | 내용 |
|--------|------|
| 카카오선물하기 | 건강식품·영양제(1~10위) + 다이어트·이너뷰티(1~10위) = 최대 20행 |
| 다이소몰 | 실시간 랭킹 TOP10 |
| 올리브영 | 판매랭킹 TOP10 |
| **카테고리통계** | 3개 플랫폼 합산 카테고리별 상품수 + 비율(%) |

#### 카테고리통계 시트 컬럼
| 컬럼 | 설명 |
|------|------|
| 카테고리 | 분류명 (기타/특수목적 포함) |
| 전체 | 해당 카테고리 총 상품수 |
| 비율(%) | 전체 수집 상품 대비 비율 |
| 카카오선물하기 | 카카오에서 해당 카테고리 상품수 |
| 다이소몰 | 다이소에서 해당 카테고리 상품수 |
| 올리브영 | 올리브영에서 해당 카테고리 상품수 |

### 월별 `data/monthly/YYYY-MM_월별취합.xlsx` — 시트 4개

| 시트명 | 내용 |
|--------|------|
| 카카오선물하기 | 월평균순위 TOP10 |
| 다이소몰 | 월평균순위 TOP10 |
| 올리브영 | 월평균순위 TOP10 |
| **카테고리통계** | 월간 TOP10 기준 카테고리 비율 |

#### 플랫폼 시트 컬럼
`순위(월평균) | 카테고리 | 상품명 | 브랜드 | 평균가격 | 평균순위 | 등장횟수`

### 월별 집계 로직
```
월평균순위 = SUM(해당월 일별순위) / 등장일수
등장횟수   = 해당월에 TOP10 안에 든 날 수
월간TOP10  = 평균순위 기준 상위 10개
```
순위에 없던 날은 제외 (결측일을 11위로 처리하지 않음)

---

## 12. requirements.txt

```
playwright==1.44.0          # 카카오 · 다이소 크롤링
playwright-stealth==1.0.6   # GitHub Actions용 (1.x API: stealth_sync)
curl_cffi>=0.7.0            # 올리브영 Cloudflare 우회 (Chrome TLS 모방)
pandas==2.2.2               # 데이터 처리
openpyxl==3.1.2             # 엑셀 저장
streamlit>=1.35.0           # 대시보드 UI
beautifulsoup4>=4.12.0      # 올리브영 HTML 파싱 (CSS 선택자 기반)
lxml>=5.0.0                 # BeautifulSoup 파서
requests>=2.32.0            # 네이버 DataLab API 호출
python-dotenv>=1.0.0        # .env 파일 로드 (API 키 관리)
```

---

## 13. 네이버 DataLab 트렌드 조사 (`run_naver_trend.py`)

### 개요
네이버 DataLab API를 이용해 건강기능식품·일반식품의 검색어 트렌드와 쇼핑 클릭 트렌드를 수집한다.
크롤링 없이 공식 API 호출만으로 최근 1년 월별 지수를 가져온다.

### API 사전 준비 (최초 1회)
1. https://developers.naver.com → **내 애플리케이션 → 애플리케이션 등록**
2. 사용 API 선택: **데이터랩(검색어 트렌드)**, **데이터랩(쇼핑 인사이트)**
3. 웹 서비스 URL: `http://localhost` (개인 사용 시)
4. 발급된 **Client ID / Client Secret**을 `.env` 파일에 입력

```
# .env  (프로젝트 루트에 생성)
NAVER_CLIENT_ID=발급받은_Client_ID
NAVER_CLIENT_SECRET=발급받은_Client_Secret
```

> `.env.example` 파일을 복사해 `.env`로 만들어 사용하세요.

### 실행
```bash
python run_naver_trend.py
```

### 수집 내용

| 단계 | API | 수집 항목 | 저장 경로 |
|------|-----|-----------|-----------|
| 1 | 검색어 트렌드 | 건강기능식품 10종 키워드 (유산균·오메가3·비타민·홍삼·콜라겐·루테인·마그네슘·밀크씨슬·아연·코엔자임Q10) | `data/naver/search/health_supplement_search_YYYYMMDD.csv` |
| 2 | 검색어 트렌드 | 일반식품 10종 키워드 (닭가슴살·단백질바·귀리·견과류·그래놀라·두부·현미·저칼로리·발효식품·샐러드) | `data/naver/search/general_food_search_YYYYMMDD.csv` |
| 3 | 쇼핑 인사이트 | 분야별 (건강식품·식품 전체) 클릭 트렌드 | `data/naver/shopping/shopping_category_YYYYMMDD.csv` |
| 4 | 쇼핑 인사이트 | 건강기능식품 10개 키워드 클릭 트렌드 | `data/naver/shopping/health_shopping_keywords_YYYYMMDD.csv` |

### 출력 예시
```
네이버 DataLab 트렌드 조사  [2025-06-26 ~ 2026-06-26]
[1/4] 건강기능식품 검색어 트렌드 수집...
[2/4] 일반식품 검색어 트렌드 수집...
[3/4] 쇼핑 인사이트 - 분야별 트렌드 수집...
[4/4] 쇼핑 인사이트 - 건강기능식품 키워드 트렌드 수집...

====================================================
  트렌드 요약 — 최근 3개월 평균 검색량 지수
====================================================
건강기능식품 검색어 TOP 5
  1. 비타민              82.3
  2. 유산균              61.5
  ...
```

### 파일 구조 추가분
```
시장조사/
├── .env                             # API 키 (git 제외 대상)
├── .env.example                     # 키 템플릿
├── run_naver_trend.py               # 실행 스크립트
├── crawlers/
│   └── naver_trend.py               # NaverDataLabAPI 클래스
└── data/
    └── naver/
        ├── search/
        │   ├── health_supplement_search_YYYYMMDD.csv
        │   └── general_food_search_YYYYMMDD.csv
        └── shopping/
            ├── shopping_category_YYYYMMDD.csv
            └── health_shopping_keywords_YYYYMMDD.csv
```

### API 제한 사항
- 검색어 트렌드: 요청당 최대 5개 키워드 그룹, 그룹당 최대 20개 키워드
- 쇼핑 인사이트 키워드: 요청당 최대 5개 키워드
- 조회 기간: 최대 2년, 시간 단위: `date`(일) / `week`(주) / `month`(월)
- API 호출 제한: 일 1,000회 (무료)

---

## 14. 유지보수 가이드

| 상황 | 조치 파일 | 구체적 수정 위치 |
|------|-----------|----------------|
| **카테고리 추가/수정** | `crawlers/classifier.py` | `CATEGORY_MAP` 리스트에 항목 추가 또는 키워드 수정 |
| 사이트 URL 변경 | `crawlers/config.py` | 해당 플랫폼 `url` 값 수정 |
| 카카오 상품 0개 | `crawlers/kakao.py` | `_JS` 내 `gc-product`, `span.txt_prdbrand`, `strong.txt_prdname` 셀렉터 확인 |
| 다이소 상품 0개 | `crawlers/daiso.py` | `_JS_COLLECT` 내 `a.detail-link` 셀렉터 확인 |
| 다이소 브랜드 누락 | `crawlers/daiso.py` | `_JS_BRAND` 내 `a.brand-area .detail-title` 갱신 |
| 올리브영 상품 0개 | `crawlers/oliveyoung.py` | `_parse()` 내 `ul.cate_prd_list`, `span.tx_brand`, `p.tx_name` 셀렉터 확인 |
| 올리브영 403 재발 | `crawlers/oliveyoung.py` | `impersonate="chrome124"` → 최신 버전으로 변경 |
| Actions push 실패 | GitHub | PAT 만료 → 재발급 후 remote URL 업데이트 |
| Actions 권한 오류 | `.github/workflows/` | `permissions: contents: write` 양쪽 yml에 있는지 확인 |
| libasound2 오류 | `.github/workflows/daily_crawl.yml` | `runs-on: ubuntu-22.04` 유지 |
| 엑셀 저장 PermissionError | - | 해당 xlsx 파일을 Excel에서 닫고 재실행 |

---

## 14. 현황 (2026-06-26 기준)

- [x] 카카오 선물하기 — 정상 (건강식품·영양제 10개 + 다이어트·이너뷰티 10개)
- [x] 다이소몰 — 정상 (실시간 랭킹 10개 + 상세페이지 브랜드 보강)
- [x] 올리브영 — 정상 (curl_cffi + BeautifulSoup, 판매랭킹 10개)
- [x] 크롤러 전면 개선 (2026-06-26)
  - 카카오: `gc-product` 웹컴포넌트 직접 선택 + MD추천 광고 필터
  - 다이소: 상세페이지 방문 브랜드 추출 (동국제약, 대웅제약 등 정확히 수집)
  - 올리브영: BeautifulSoup CSS 선택자 파싱 + rowsPerPage 8→24
- [x] 카테고리 분류 시스템 도입 (2026-06-26)
  - 11개 카테고리 (멀티비타민, 비타민C, 비타민D, 마그네슘, 루테인/눈건강, 혈행, 홍삼/인삼, 다이어트, 유산균, 피부/미용, 기타)
  - 일별·월별 xlsx에 `카테고리통계` 시트 자동 생성 (플랫폼별 상품수 + 비율%)
  - `crawlers/classifier.py` 키워드 수정만으로 카테고리 즉시 반영
- [x] GitHub Actions 권한 수정 — `permissions: contents: write` 추가 (2026-06-26)
- [x] Streamlit 대시보드 완성 (오늘 랭킹 / 월별 리포트 / 수동 수집)
- [x] GitHub Actions 자동화 세팅 완료 (매일 09:00 KST)
- [ ] GitHub Actions 실제 자동 수집 결과 확인 (Actions 탭에서 초록 체크 확인)
- [ ] 월별 리포트 실데이터 검증 (데이터 누적 후 월말에 확인)
- [ ] 카테고리 지속 추가 예정 (오메가3 분리, 철분, 칼슘 등)
