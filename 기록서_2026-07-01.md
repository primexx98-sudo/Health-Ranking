# 작업 기록서 — 2026-07-01

프로젝트: 건강기능식품 판매순위 크롤러  
작성일: 2026-07-01

---

## 1. 로컬 git pull 자동화

**문제:** GitHub Actions가 정상 수집·push 했음에도 로컬에 파일이 없어 보임 (git pull 미실행)

**원인:** GitHub Actions는 클라우드에서 수집 후 GitHub에 push까지 완료하지만, 로컬 PC는 별도로 pull 해줘야 반영됨

**조치:** Windows 작업 스케줄러에 로그인 시 자동 git pull 등록
- 작업명: `HealthRanking_GitPull`
- 트리거: 로그인 시 (AtLogOn)
- 실행: `git pull` in `C:\Users\thefuture_brand\Desktop\업무\시장조사`
- 창 없이 백그라운드 실행

---

## 2. 월별 취합(monthly_aggregate) 누락 수정

**문제:** 매월 1일 월별 취합 파일이 자동 생성되지 않음

**원인:** `daily_crawl.yml`과 `monthly_aggregate.yml`이 동일 시각(UTC 00:00) 동시 실행
→ daily가 먼저 push → monthly의 push가 remote 충돌로 조용히 실패

**조치:** `monthly_aggregate.yml`에 push 전 `git pull --rebase` 추가
```yaml
git pull --rebase
git push
```

**이번 달 처리:** 로컬에서 수동 실행 후 push 완료

---

## 3. 월별 순위 산출방식 개선

### 기존 문제
평균순위 1위인데 등장횟수 1~2번인 제품이 월간 TOP10 상위에 오는 정확도 문제

### 인터뷰 결과 및 결정사항

| 항목 | 결정 |
|------|------|
| 처리 방식 | B) 순위 하향 — 점수 공식으로 등장 적은 상품을 자연스럽게 하위에 배치 |
| 점수 체계 | 1위=5점, 2위=4.5점 … 10위=0.5점 (0.5점 차등) |
| 등장일 처리 | 미등장일 0점, 실제 수집일수 기준 등장률 산정 |
| 가중치 | 순위 70% + 꾸준함 30% |
| 출력 컬럼 | 기존 유지 (평균순위 표시, 점수 컬럼 없음) |

### 최종 공식

```
일별점수  = (11 - 순위) × 0.5
평균점수  = SUM(일별점수) / 등장횟수
등장률    = 등장횟수 / 실제수집일수  ← 달력 일수 아님
월간점수  = 평균점수 × 0.7 + (등장률 × 5) × 0.3
월간TOP10 = 월간점수 기준 정렬 (표시는 평균순위)
```

### 결정 과정
1. 첫 시도: `월간점수 = 평균점수 × 등장률` (분모=달력 30일) → 등장률이 너무 낮아 등장 적은 제품 과도 패널티
2. 분모를 실제 수집일수로 변경 → 개선
3. 가중치 50:50 시도 → 등장 많은 저순위 제품이 1위 제품을 역전하는 문제
4. **가중치 70:30 확정** → 순위 질 우선, 꾸준함 보조 반영

### 변경 파일
- `monthly_aggregate.py` — 점수 공식 및 분모 수정
- `설계서.md` — 집계 로직 섹션 업데이트
- `.github/workflows/monthly_aggregate.yml` — git pull --rebase 추가

---

## 커밋 이력

| 커밋 | 내용 |
|------|------|
| `7fb9a00` | data: 2026-06 월별 취합 (로컬 수동) |
| `c7b9629` | fix: monthly_aggregate push 전 git pull --rebase 추가 |
| `76ba530` | docs: 설계서 월별취합 충돌 수정 이력 반영 |
| `de167be` | feat: 월별 순위 점수 기반 산출 (순위70%+꾸준함30%, 실제수집일수 기준) |
