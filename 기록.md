# 기록 (히스토리 · 의사결정 로그)

> 설계서.md는 현재 동작 중인 로직/구조만 담는다. "왜 이렇게 바뀌었는지", "과거에 뭘 했는지"는 여기에 기록한다.

---

## 프로젝트 결정 이력

**2026-07-01 — health-trend 연동 취소**
건강기능식품 트랜드 대시보드(health-trend)에서 이 저장소를 raw 파일로 연동하는 `ecommerce_collector.py`를 개발했으나, health-trend 프로젝트 자체가 보안 이슈로 폐쇄 결정되어 연동하지 않기로 확정. 이 저장소의 Public/Private 전환은 더 이상 health-trend에 영향받지 않음.

---

## 월별 집계 점수 공식 — 결정 과정 (2026-07-01 인터뷰)

**기존 문제:** 평균순위 1위인데 등장횟수 1~2번인 제품이 월간 TOP10 상위에 오는 정확도 문제

| 항목 | 결정 |
|------|------|
| 처리 방식 | B) 순위 하향 — 점수 공식으로 등장 적은 상품을 자연스럽게 하위에 배치 |
| 점수 체계 | 1위=5점, 2위=4.5점 … 10위=0.5점 (0.5점 차등) |
| 등장일 처리 | 미등장일 0점, 실제 수집일수 기준 등장률 산정 |
| 가중치 | 순위 70% + 꾸준함 30% |
| 출력 컬럼 | 기존 유지 (평균순위 표시, 점수 컬럼 없음) |

**시행착오**
1. 첫 시도: `월간점수 = 평균점수 × 등장률` (분모=달력 30일) → 등장률이 너무 낮아 등장 적은 제품 과도 패널티
2. 분모를 실제 수집일수로 변경 → 개선
3. 가중치 50:50 시도 → 등장 많은 저순위 제품이 1위 제품을 역전하는 문제
4. 가중치 70:30 확정 → 순위 질 우선, 꾸준함 보조 반영

**2026-07-02 — 최소 등장횟수 필터 추가**
등장횟수 3회 미만 상품은 월간 랭킹 대상에서 제외 (1~2회만 등장해도 평균순위가 좋으면 상위권에 오르는 문제 방지). 다이소몰처럼 표본이 적은 플랫폼에서 3회 필터만 적용하면 TOP10이 안 채워지는 문제가 발견되어, 필터 후 10개 미만이면 최소등장 기준을 2회 → 1회로 순차 완화하도록 추가 수정.

---

## 작업 이력 (일자별)

- **2026-06-25** — 프로젝트 시작. 카카오·다이소·올리브영 3개 플랫폼 크롤러 최초 구현
- **2026-06-26** — 크롤러 전면 개선
  - 카카오: `gc-product` 웹컴포넌트 직접 선택 + MD추천 광고 필터
  - 다이소: 상세페이지 방문 브랜드 추출 (동국제약, 대웅제약 등 정확히 수집)
  - 올리브영: BeautifulSoup CSS 선택자 파싱 + rowsPerPage 8→24
  - 카테고리 분류 시스템 도입 (11개 카테고리, `카테고리통계` 시트 자동 생성)
  - GitHub Actions 권한 수정 — `permissions: contents: write` 추가
  - Streamlit 대시보드 완성, Actions 자동화 세팅 완료 (매일 09:00 KST)
- **2026-06-29** — 올리브영 Cloudflare 차단 대응
  - 6/26~28 GitHub Actions에서 올리브영 HTTP 403 발생 (`chrome124` 차단)
  - `impersonate="chrome124"` → `"chrome131"` 변경으로 해결, 누락분 수동 백필
- **2026-06-30** — 올리브영 Cloudflare 재차단 대응
  - `impersonate="chrome131"` → `"chrome146"` / User-Agent도 146으로 갱신
  - 6/29 누락 데이터 로컬 패치 완료
- **2026-07-01** — 월별 취합 안정화
  - monthly_aggregate push 충돌 수정: 매월 1일 daily_crawl과 동시 실행 시 git push 충돌 → `git pull --rebase` 추가
  - 로컬 git pull 자동화 (Windows 작업 스케줄러, 로그인 시 자동 실행)
  - 월별 순위 점수 기반 산출 도입 (순위70%+꾸준함30%, 상세는 위 결정 과정 참고)
- **2026-07-02** — 구조 개선 및 정확도 개선
  - `data/daily` 월별 하위폴더 구조 도입 (`data/daily/YYYY-MM/YYYY-MM-DD.xlsx`), 기존 파일 `git mv`로 이동
  - daily_crawl push 충돌 수정: 7/1 예약 실행이 push 충돌로 실패했던 것을 발견, `git pull --rebase` 추가
  - 월별 랭킹 최소 등장횟수 3회 필터 + 소급 완화 로직 추가 (상세는 위 참고)
  - 설계서.md / README.md 간략화, 기록.md 신설 (본 문서)

---

## 커밋 이력 (주요 지점)

| 커밋 | 내용 |
|------|------|
| `7fb9a00` | data: 2026-06 월별 취합 (로컬 수동) |
| `c7b9629` | fix: monthly_aggregate push 전 git pull --rebase 추가 |
| `de167be` | feat: 월별 순위 점수 기반 산출 (순위70%+꾸준함30%, 실제수집일수 기준) |
| `4be77bd` | refactor: data/daily 월별 하위폴더 구조 도입 |
| `e050f1f` | fix: daily_crawl push 충돌 수정 |
| `b54b667` | feat: 월별 랭킹에 등장횟수 3회 미만 제외 필터 추가 |
| `18ac4a1` | fix: 월별 랭킹 등장횟수 필터 소급 완화 |

> 전체 커밋 이력은 `git log`로 확인. 위 표는 주요 로직 변경 지점만 발췌.
